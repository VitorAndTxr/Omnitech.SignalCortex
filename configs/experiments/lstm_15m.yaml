data:
  pair_name: BTCUSDT
  timeframe: "15m"

model:
  type: lstm
  window_size: 96
  hidden_size: 128
  num_layers: 2
  dropout: 0.3
  bidirectional: true
  use_attention: true

training:
  epochs: 100
  batch_size: 256
  learning_rate: 0.001
